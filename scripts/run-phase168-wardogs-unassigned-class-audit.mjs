import fs from 'node:fs';
import crypto from 'node:crypto';
const EXPECTED_SHA='7cdf33dc7aead9760f00ca3abbc30031283c6543';
function gitBlobSha(bytes){return crypto.createHash('sha1').update(Buffer.from('blob '+bytes.length+String.fromCharCode(0))).update(bytes).digest('hex')}
function webpInfo(bytes){
 if(bytes.length<30||bytes.subarray(0,4).toString()!=='RIFF'||bytes.subarray(8,12).toString()!=='WEBP'||bytes.readUInt32LE(4)+8!==bytes.length)return {ok:false};
 let offset=12,hasAlpha=false,hasImage=false,width=0,height=0;
 while(offset<bytes.length){if(offset+8>bytes.length)return {ok:false};const type=bytes.subarray(offset,offset+4).toString(),size=bytes.readUInt32LE(offset+4),start=offset+8,end=start+size;if(end>bytes.length)return {ok:false};if(type==='VP8X'){if(size<10)return {ok:false};width=1+bytes[start+4]+(bytes[start+5]<<8)+(bytes[start+6]<<16);height=1+bytes[start+7]+(bytes[start+8]<<8)+(bytes[start+9]<<16);}if(type==='ALPH')hasAlpha=true;if(type==='VP8 '||type==='VP8L')hasImage=true;offset=end+(size&1)}
 return {ok:offset===bytes.length,hasAlpha,hasImage,width,height};
}
export function runPhase168WardogsUnassignedClassAudit(){
 const issues=[],warnings=[];
 const index=fs.readFileSync('index.html','utf8'),gallery=fs.readFileSync('assets/wardogs-v1.js','utf8'),manager=fs.readFileSync('assets/wardogs-manager-v1.js','utf8'),data=fs.readFileSync('assets/wardogs-data-v1.js','utf8'),galleryCss=fs.readFileSync('assets/wardogs-v1.css','utf8'),managerCss=fs.readFileSync('assets/wardogs-manager-v1.css','utf8'),auth=fs.readFileSync('src/cf-v111-auth.js','utf8'),workflow=fs.readFileSync('.github/workflows/deploy-cloudflare-production.yml','utf8');
 const framePath='assets/wardogs-frames/unassigned.webp',classList="['assault','medic','recon','support','driver','pilot','unassigned']";
 for(const token of ['<strong>7 CLASS CHANNELS AVAILABLE</strong>','data-wardogs-class="unassigned"','<span class="wd-class-code">UNA // 07</span><span class="wd-class-name">미배치</span>','assets/wardogs-v1.css?v=1.0.0-phase125-mobile129-portrait138-gallery136-aperture152-medicaperture165-classapertures166-unassigned168-framepreload169','assets/wardogs-manager-v1.css?v=1.0.0-phase127-mobile129-frame140-frame144-aperture152-medicaperture165-classapertures166-unassigned168','assets/wardogs-data-v1.js?v=1.0.0-phase120-webview130-zoom138-unassigned168','assets/wardogs-v1.js?v=1.0.0-phase125-portrait138-soopid141-frames150-aperture152-assault166-medic165-recon163-support166-driver161-pilot166-unassigned168-framepreload169','assets/wardogs-manager-v1.js?v=1.0.0-phase127-touchfix-webview130-frame140-frame144-frames150-aperture152-assault166-medic165-recon163-support166-driver161-pilot166-unassigned168'])if(!index.includes(token))issues.push('Phase 168 index token missing: '+token);
 if(index.indexOf('data-wardogs-class="unassigned"')<index.indexOf('data-wardogs-class="pilot"'))issues.push('Phase 168 unassigned tab must be last after pilot');
 for(const [source,name,tokens] of [[gallery,'gallery',["{id:'unassigned',name:'미배치',code:'UNA',label:'미배치'}","unassigned:'assets/wardogs-frames/unassigned.webp?v=phase168'","window.__mwsWardogsUnassignedV168='seventh-class-last-slot';"]],[manager,'manager',["{id:'unassigned',label:'미배치'}","unassigned:'assets/wardogs-frames/unassigned.webp?v=phase168'","window.__mwsWardogsUnassignedV168='seventh-class-last-slot';"]],[data,'data',["const CLASS_IDS=Object.freeze("+classList+");","window.__mwsWardogsClassSetV168='unassigned-last';"]],[auth,'auth',["const WARDOGS_CLASS_IDS=Object.freeze("+classList+");"]]])for(const token of tokens)if(!source.includes(token))issues.push('Phase 168 '+name+' token missing: '+token);
 if(!galleryCss.includes('grid-template-columns:repeat(7,minmax(0,1fr))'))issues.push('Phase 168 desktop class tabs are not seven columns');
 for(const [source,name,selector] of [[galleryCss,'gallery','.wardogs-portrait-stage-v134[data-wardogs-class="unassigned"] .wardogs-portrait-clip-v152'],[managerCss,'manager','.wardogs-manager-drop-v122[data-wardogs-class="unassigned"] .wardogs-manager-portrait-clip-v152']]){if(!source.includes(selector))issues.push('Phase 168 '+name+' aperture selector missing');if(!source.includes('clip-path:inset(8.2% 11.5% 22.3% 12.1%);'))issues.push('Phase 168 '+name+' aperture geometry missing')}
 if(!fs.existsSync(framePath))issues.push('Phase 168 unassigned frame missing');else{const bytes=fs.readFileSync(framePath),actual=gitBlobSha(bytes),info=webpInfo(bytes);if(actual!==EXPECTED_SHA)issues.push('Phase 168 unassigned frame SHA mismatch: '+actual);if(!info.ok||!info.hasAlpha||!info.hasImage||info.width!==360||info.height!==480)issues.push('Phase 168 unassigned frame WebP invalid: '+JSON.stringify(info));if(bytes.length<8000||bytes.length>30000)issues.push('Phase 168 unassigned frame performance size guard failed: '+bytes.length)}
 for(const token of ['run-phase168-wardogs-unassigned-class-audit.mjs','unassigned.webp?v=phase168','assault medic recon support driver pilot unassigned',EXPECTED_SHA])if(!workflow.includes(token))issues.push('Phase 168 workflow verification missing: '+token);
 const summary={phase:168,name:'wardogs-unassigned-seventh-class',issues,warnings,pass:issues.length===0};console.log(JSON.stringify(summary));if(issues.length)process.exitCode=1;return summary;
}
if(import.meta.url===`file://${process.argv[1]}`)runPhase168WardogsUnassignedClassAudit();
